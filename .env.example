# Local development
PORT=4000
VITE_API_BASE=

# Catalyst deployment
# Set VITE_API_BASE=/server/sherlock_api before building if API Gateway is not used.

# Optional future AI provider keys. Never commit real values.
OPENAI_API_KEY=
ZOHO_CATALYST_PROJECT_ID=
